import { createMuiTheme } from "@material-ui/core";

export const CustomTheme = createMuiTheme({
  palette: {
    primary: {
      light: "#5490FF",
      main: "#4C82E6",
      dark: "#2A4880",
    },
  },
});
